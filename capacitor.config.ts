import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'binance-open-order-mobile-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
